# Tests use synthetic data and substitute optional model inference.
-r requirements-runtime.txt
pytest==9.1.1
httpx==0.28.1
# Keep the existing pandas 2.x data contract; a 3.x migration is separate work.
pandas==2.3.3
